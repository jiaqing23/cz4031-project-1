Installation Guide:

1. Make sure your C++ version is at least C++ 17. 

Two ways to view results:

1. Run `make run` in the project folder's terminal to view all experiment results in the terminal.
2. Run `make run > results.txt` in the project folder's terminal to generate a `results.txt` file containing all experiment results. 